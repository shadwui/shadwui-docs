---
title: Textarea
description: Displays a callout for user attention.
---

import {
  Tab,
  DocTabs,
  Tabs,
  TabsList,
  TabsContent,
  TabsTrigger,
  Step,
  Steps,
  CodeBlock,
  Separator,
  BannerDemo,
  CodePreview,
  ComponentPage,
  CodeBlockWrapper,
} from "../../components/index";

## Preview
The Badge component is used to display important messages to users. It comes with several sub components to structure the content effectively.
<CodePreview
  sourcePath="src/default"
  componentName={"alert"}
  minimumCodeHeight="300px"
/>

## Installation 
<Tabs defaultValue="cli">
  <TabsList className="pb-0 h-10 rounded-md bg-hidden">
    <TabsTrigger
      value="cli"
      className="rounded-sm px-2 py-1.5 data-[state=active]:text-chai data-[state=active]:underline data-[state=active]:underline-offset-10 data-[state=active]:decoration-3 hover:cursor-pointer data-[state=active]:bg-hidden data-[state=active]:shadow-none"
    >
      CLI
    </TabsTrigger>
    <TabsTrigger
      value="manual"
      className="rounded-sm px-2 py-1.5 data-[state=active]:text-chai data-[state=active]:underline data-[state=active]:underline-offset-10 data-[state=active]:decoration-3 hover:cursor-pointer data-[state=active]:bg-hidden data-[state=active]:shadow-none"
    >
      Manual
    </TabsTrigger>
  </TabsList>
  <Separator />
  <TabsContent value="cli" className="p-0 mt-0">
    <DocTabs items={["pnpm", "npm", "yarn", "bun"]}>
      <Tab value="pnpm">
        ```bash
          pnpm dlx shadcn@latest add http://localhost:3000/r/hello-world.json
        ```
      </Tab>
      <Tab value="npm">
        ```bash
          pnpm dlx shadcn@latest add http://localhost:3000/r/hello-world.json
        ```
      </Tab>
      <Tab value="yarn">
        ```bash
          pnpm dlx shadcn@latest add http://localhost:3000/r/hello-world.json
        ```
      </Tab>
      <Tab value="bun">
        ```bash
        pnpm dlx shadcn@latest add http://localhost:3000/r/hello-world.json
        ```
      </Tab>
    </DocTabs>
  </TabsContent>

  <TabsContent value="manual" className="p-0 mt-0">
   <Steps>
    <Step>
      **Install the following dependencies:**

      <DocTabs items={["pnpm", "npm", "yarn", "bun"]}>
        <Tab value="pnpm">
          ```bash
          pnpm dlx shadcn@latest add http://localhost:3000/r/hello-world.json
          ```
        </Tab>
        <Tab value="npm">
          ```bash
          pnpm dlx shadcn@latest add http://localhost:3000/r/hello-world.json
          ```
        </Tab>
        <Tab value="yarn">
          ```bash
          pnpm dlx shadcn@latest add http://localhost:3000/r/hello-world.json
          ```
        </Tab>
        <Tab value="bun">
          ```bash
          pnpm dlx shadcn@latest add http://localhost:3000/r/hello-world.json
          ```
        </Tab>
      </DocTabs>

    </Step>
    <Step>
      **Copy and paste the following code into your project.**

      ```typescript
      import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
      ```

    </Step>

     <Step>
        **Update the import paths to match your project setup.**
    </Step>
   </Steps>
  </TabsContent>
</Tabs>

## Usage
```jsx
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/badge"
```

## Examples
<ComponentPage 
  sourcePath="src/components/textareas"
  files={[
    "textarea-01",
    "textarea-02", 
    "textarea-03",
    "textarea-04",
    "textarea-05",
    "textarea-06",
    "textarea-07",
    "textarea-08",
    "textarea-09",
    "textarea-10",
    "textarea-11",
    "textarea-12",
    "textarea-13",
    "textarea-14",
    "textarea-15",
    "textarea-16",
    "textarea-17",
    "textarea-18",
    "textarea-19"
  ]}
/>
